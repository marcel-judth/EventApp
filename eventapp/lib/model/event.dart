class Event{
  final String name;
  final String description;
  final int maxMembers;
  final longitude;
  final latitude;

  Event({this.name, this.description, this.maxMembers, this.longitude, this.latitude});
}